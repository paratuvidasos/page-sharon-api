export enum BannerPlacement {
  /** Modal que se muestra al aterrizar en la home, con overlay oscuro y botón de cierre. */
  WELCOME_MODAL = "WELCOME_MODAL",
  /** Sección/carrusel persistente dentro de la home (no depende de si es la primera visita). */
  HOME_SECTION = "HOME_SECTION",
}
